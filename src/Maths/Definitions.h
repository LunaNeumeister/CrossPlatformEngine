#pragma  once
//Epsilion used for float comparison
static const float Epsilon = 0.0001f;
static const float EPSILON = 0.0001f;

static const float _PI_ = 3.14159265f;
bool EqualF(float one, float two);
float FloatAdd(float one, float two);
